<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link href='/css/show.css' rel='stylesheet' type='text/css'>
  <title>Short URL</title>
</head>

<body>
  <% include partials/_header %>
  <main>
    <div>
      <h1 id='restaurantName'><%= restaurantName %></h1>
    </div>
    <div class="menu">
      <h3>Menu</h3>
      <form method="POST" action="/browse/<%= restaurantUrlName %>">
        <input type='hidden' name='restaurantName' value='<%= restaurantName %>'/>
        <ul>
        <% for (restaurantItem of restaurant) { %>
          <li>
            <input type='hidden' name='cost' value='<%= (restaurantItem.cost_in_cents / 100).toFixed(2) %>'/>
            <input type='hidden' name='item' value='<%= restaurantItem.name %>'/>
            <h4><%= restaurantItem.name %></h4></input>
            <p><%= restaurantItem.description %></p>
            <p>$<%= (restaurantItem.cost_in_cents / 100).toFixed(2) %></p>
            <div class='iterator'>
              <button class='minus' disabled="disabled">-</button>
              <input type='number' value='0' min='0' name='quantity' class='menu-item-input'/>
              <button class='plus'>+</button>
            </div>
          </li>
            <% } %>
          </ul>
          <button id='submit' type="submit" value="submit">Submit</button>
      </form>
    </div>
  </main>

  <script src='/scripts/iterator.js'></script>
  <!-- <script>
    let restaurant = document.getElementById('restaurantName').innerHTML;
    let quantities = document.getElementsByClassName('menu-item-input');
    let data = JSON.parse(localStorage.orderObj);
    console.log(data);

    if (data[restaurant]) {
      for (let i = 0; i < quantities.length; i++) {
        
      }
    } else {
      console.log('WOOO');
    }
  </script> -->
</body>

</html>
